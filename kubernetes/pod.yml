apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    name: nginx
spec:
  containers:
    - name: nginx
      image: sameersbn/nginx:1.8.1-8
      ports:
        - containerPort: 80
          protocol: TCP
